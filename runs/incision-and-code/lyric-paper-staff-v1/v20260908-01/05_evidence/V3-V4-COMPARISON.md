# 《切口与暗号》前奏：v3 与候选 v4 对照

本目录是 `MV03-attempt1` 的独立、可选审阅包。v4 **未获批准、不可上传、不会替换或覆盖 v3**。

| 观察点 | 既有 v3 | 候选 v4 短样 |
|---|---|---|
| 前奏 0–约9.43秒 | 既有五线谱画面 | 0–3 秒铺出谱线；3–7 秒只在既有分析的实际音乐事件处出现装饰音符；7–9.43 秒收拢到阅读区 |
| 第一个演唱字 | v3 时间轴的“公”= 9.430 秒 | 同一实测点触发“公”的聚焦；后续字符沿用既有逐字时间 |
| 音频 | 已有完整视频音轨 | 复用同一个源 MP3 的 0–15.5 秒，速度 1.0×；未改音频结构 |
| 可审阅证据 | 旧成片 | `03_render/*v3-intro-comparator.mp4` 与 `05_evidence/v3-943.jpg` |

审阅问题：手机尺寸是否能看出歌曲氛围；09.430 秒首字是否自然成为焦点；谱线／音符变化是否看起来与音乐事件相连。
